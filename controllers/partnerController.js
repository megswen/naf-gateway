var dotenv = require('dotenv');
dotenv.config();
const axios = require('axios');

const searchPartnerById = async(partnerId) => {
    try {

    } catch(error) {
        return new Error(error.stack);
    }
}